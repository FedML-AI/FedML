apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fedml-model-inference-ingress.fullname" . }}-test-connection"
  labels:
    {{- include "fedml-model-inference-ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fedml-model-inference-ingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
